# LinguaType 产品说明

## 产品定位

LinguaType 是面向中文母语英语学习者的“输入法式英文表达助手”。它帮助用户在真实写作现场，把光标所在当前句中的中文意图、中英混写和不自然英文表达，转换成更自然的英文句子。

LinguaType 不是通用翻译器、聊天机器人、作文生成器、全文批改器、作文评分工具、真实系统输入法、Chrome 扩展或云同步产品。

## 当前版本

当前版本是 v0.3.1。

v0.3.1 在 v0.3“文章地图”基础上新增自动预检查：

- 系统会在本地感知段落数量、段落指纹、正文 hash、主题和大纲变化，并判断地图是 `empty`、`fresh`、`stale`、`needs_check`、`checking`、`ready` 或 `failed`。
- 默认“安静时预检查”只在文章至少 2 段、至少 120 个英文词、用户停止输入约 10 秒、变化幅度足够、当前没有其他 AI 请求、API 设置可用且未超过节流/次数限制时，后台调用 `POST /api/check-document-map`。
- 自动预检查只更新 `linguatype.documentMapCache.v1` 和入口轻状态，例如“文章地图 · 可检查”“文章地图 · 整理中”“文章地图 · 3 个发现”“文章地图 · 可能已过期”。
- 自动预检查不会自动展开文章地图、不会定位段落、不会触发“查看建议”或“检查本段”、不会改写正文、不会评分、不会保存学习数据，也不会触发 `/api/extract-learning`。
- “触发设置”新增文章地图自动检查模式：关闭、仅提醒、安静时预检查、手动优先。

v0.3 在 v0.2.8 的沉浸式写作工作台上新增“文章地图”：

- 用户手动点击“检查文章地图”后，系统按正文分段生成或打开全文结构地图；空行是明确分段，没有空行时也会识别单次手动换行形成的自然段落。
- 文章地图展示全文主旨、结构判断、全文问题、段落角色、段落关系、段落健康摘要和优先修改建议。
- 面板采用可折叠大纲形态，宽屏下与正文左右对照显示；用户可以定位相关段落、查看轻量段落健康建议，或进入“检查本段”。
- 正文仍保持一个连续长文本输入面，不拆成段落卡片，不改变 Word / Typora 式输入稳定性。
- 文章地图不返回全文改写、不评分、不自动应用、不保存学习数据，也不触发 `/api/extract-learning`。
- 新增 `POST /api/check-document-map` 和本地缓存 `linguatype.documentMapCache.v1`。

v0.2.8 在 v0.2.7 沉浸式工作台基础上继续收敛交互：

- 主界面进一步收敛为沉浸式 Typora 式一体化工作台，正文编辑器保持第一视觉优先级。
- 标题直接在正文流中编辑，删除重复主题行和显式编辑按钮。
- 写作存档侧栏升级为固定的本地文章库，支持搜索、排序、领域筛选、领域堆栈和拖放分类。
- 主题切换和 `API 设置` 移到右上角；数据管理、触发设置和快捷键帮助移到左侧低频入口。
- 表达库取消写作模式关联，改为难度 1-5 和类型筛选，并把个人词典并入类型选项。
- 写作习惯改为更自然的报告流，趋势图和示例使用规整前后对照。
- 写作领域可在用户切换文章后低频自动分类；用户手动拖放分类后不再自动覆盖。
- 文章主题以 H1 风格融入正文流，正文输入区回到单块原生长文本写作面，优先保证光标、选区、输入法和追加输入稳定。
- 中文片段被视为表达占位符；只有完整句子稳定后才后台准备建议，不在刚输入中文时打断。
- 当前句建议先以目标句右侧的低干扰入口出现，用户打开后才让目标句临时独立成段并显示内联卡片，仍必须由用户采纳或忽略。
- 大纲检查仍只在用户确认保存主题或大纲修改后调用 `/api/check-outline`。
- 右侧不再承担常驻管理面板，只保留轻量主题切换；表达库、写作习惯和设置改为左侧入口，在中间主舞台打开。
- `API 设置`、触发与打扰设置、数据管理合并为统一设置页；个人词典并入表达库的类型选项。
- 主题切换只显示 `浅色`、`深色`、`跟随系统`，并补齐深色布局变量。
- 侧栏品牌图片随实际主题切换；展开侧栏使用完整 wordmark，折叠侧栏使用单字母 mark，品牌图片不参与正文或模型链路。
- 选区操作改为贴近选区的轻量功能条，支持解释、保存和复制，不再先显示单独小图标。
- 写作区移除文本校对右侧 tag 和独立校对卡片；底部状态栏保留文本统计，并把文章地图轻状态放在原校对提示位置。
- 模式、强度和主题等高频状态控制保持轻量下拉或轻量弹层，点击外部或 Escape 可收起。
- 表达库、写作习惯、数据管理、触发设置与 API 设置动作统一给中文优先的轻量反馈；清空/删除类动作保留确认，API 测试失败仅反馈脱敏后的 key 信息，不改变模型请求行为。
- 用户界面默认使用中文，不再使用 `数据管理 Data Control`、`表达库 Learning Library` 这类装饰性中英文并排标题；API、JSON、localStorage 等必要技术名词除外。
- 当前句建议增加本地缓存。已生成过的中文占位句建议会按文档、原句、模式、强度和领域复用，重新打开页面时不因页面加载重复请求模型。
- 当前句增强触发收敛为 `Ctrl/Cmd + Enter` 或按钮入口，旧 `Ctrl/Cmd + J` 保持 inactive；折叠侧栏保留增强当前句图标按钮。
- 段落健康保持开启，只允许 Apply 后触发或段落完成后触发；段落流畅度只通过显式 `检查本段` 触发，`Ctrl/Cmd + K` 直接进入当前段检查。
- 新增表达复现提示。用户自然写出表达库中已沉淀的短语或搭配时，正文对应文本会出现一次卡片压印式低打扰微动效，不保留常驻标记，也不再显示 hover/focus 说明卡片。
- 表达库使用统一小卡片网格承载长期积累的表达资产；卡片保持等宽铺开，来源句默认收起，点击卡片在原位展开完整来源，不改变页面整体排版。

历史版本细节只保留在 `CHANGELOG.md`。`PRODUCT.md` 只描述当前产品规则，避免旧版分段输入、setup 抽屉或旧面板形态被误读为当前形态。

## 核心流程

```text
用户写作
  -> 输入英文或中英混写
  -> 触发当前句增强
  -> POST /api/enhance-fast
  -> 代码生成 diff
  -> Apply / Cancel
  -> Apply 后替换捕获的当前句
  -> 应用修改后后台提取表达库和 Correction Events
```

当前句增强仍只处理光标所在当前句，不改写整段，不生成新论点，不自动 Apply，不在用户确认前保存学习数据。

## 写作准备

写作准备页只收集用户提供的写作上下文：

- 写作领域
- 文章主题
- 大纲点

主题刷新只从本地预设主题中抽取，不调用 LLM，不生成正文，也不自动修改大纲。

## 长文本写作

主写作界面使用一个连续的长文本正文输入面。用户输入、删除、换行、点击定位、选中内容和继续输入都应接近 Word、Typora 或飞书文档的自然写作体验。

中文片段不是错误，而是用户卡住时的表达占位符。系统只在句号、问号、感叹号、换行或稳定停顿后处理包含中文的完整句子，并先查本地建议缓存。命中缓存时只恢复句旁入口和内联卡片；未命中时才后台请求 `/api/enhance-fast`。建议准备好后，正文只显示句旁轻入口；用户打开后，目标句临时单独成为一段，卡片显示自然英文、中文表达到英文表达的映射和一个可复用结构。

底层草稿仍保存为 `linguatype.writingDraft.v1` 中的单个文本，继续保持 current-sentence extraction、range replacement、Apply 冲突检测和本地草稿恢复的既有行为。文章主题和 Writing Setup 元数据不并入正文 `text`。

表达复现提示让表达库以被动强化的方式参与写作流。系统只在本地匹配 `phrase` 和 `collocation`，不匹配普通单词，不调用模型，不推荐候选，不改正文，也不保存新的学习数据。它只是在用户已经写出已学表达后，用一次极轻的卡片压印式原位动效加深记忆；该提示不接管 hover/focus，也不弹出说明卡片。

## 写作存档

写作存档用于保存一次次本地写作内容。每个存档包含标题、正文和 Writing Setup。存档标题可以单独重命名，不会自动改写文章主题。

切换存档会保存当前正文和 setup，但不会保存学习数据，不触发表达库、Correction Events 或 `/api/extract-learning`。

写作存档侧栏可以折叠。存档条目的 `...` 菜单支持重命名和删除。删除只影响 `linguatype.writingArchives.v1` 中对应的本地草稿，不会清空表达库、Correction Events 或旧 draft/setup key。

左侧区域固定在视口内，不跟随中间正文滚动。表达库、写作习惯和设置通过左侧入口切换中间主舞台；右侧不再放置低频工具栏。

## 大纲检查

用户在编辑器内联修改主题或大纲后，只有点击确认保存才调用 `/api/check-outline`。该检查只判断大纲是否明显偏离文章主题、重复或过少。

规则：

- 不写作文。
- 不生成段落。
- 不决定用户论点。
- 不自动修改大纲。
- 无问题时不显示提示。
- 有问题时只显示简短中文建议。

## 文章地图

文章地图是全文结构检查入口。用户写到至少两个非空段落后，可以点击“检查文章地图”生成或打开可折叠大纲，先确认 AI 对全文主旨和结构的理解，再决定优先处理哪一段。系统也可以在“安静时预检查”模式下静默准备最新地图缓存，但只在入口处显示轻提示，用户点击后才查看完整结果。

文章地图只做三件事：

- 诊断：指出主旨偏移、段落重复、段落跳跃、过渡不足、论证递进不清、题目/大纲回应不足等全文结构问题。
- 定位：点击问题或段落节点时，正文滚动并短暂高亮对应段落。
- 调度：段落节点内提供“查看建议”和“检查本段”，分别进入轻量段落健康和手动 Paragraph Flow；“查看建议”在当前段落卡片内展开轻量健康摘要，不弹出右下角面板，也不进入段落改写检查。

文章地图不做全文改写、不评分、不生成新论点、不自动改段落顺序、不自动生成过渡句、不保存表达库或写作习惯数据。正文修改后，旧地图只提示可能过期；如果自动预检查符合条件，可以静默刷新缓存，但不得自动展开面板或进入段落检查。

用户点击“检查本段”或从折叠侧栏触发当前段检查时，结果进入文章地图内的二级检查界面，不再在页面底部显示段落工具。二级界面展示结构/流畅度问题、语法/拼写/标点等有价值的细节问题、建议改写和手动 Apply；空格类细碎排版问题不单独铺满列表，模型运行中必须显示明确的运行态动效。

宽屏打开文章地图时，主写作区使用左右对照布局：地图列用于浏览结构，正文列保持连续长文本编辑器，方便边看结构边定位原文。对照区内地图列和原文列各自独立滚动，隐藏可见滚动条，原文滚动不能带着文章地图一起移动。窄屏可以回退为上下排列，但不得把正文拆成段落输入卡片。

## 本地优先

v0.3.1 数据保持 localStorage-only，包括 API 设置、writing draft、Writing Setup、Writing Archives、Theme Settings、表达库、Correction Events、Paragraph Health cache、Placeholder Suggestion cache、Document Map cache、Trigger Settings 和个人词典。当前建议缓存键为 `linguatype.placeholderSuggestionCache.v1`，文章地图缓存键为 `linguatype.documentMapCache.v1`；文章地图缓存会记录正文 hash、主题/大纲 hash、段落指纹、freshness、生成时间、自动检查时间和会话内自动检查次数。

当前范围不引入登录、数据库、云同步、支付、导入系统或后端持久化。
