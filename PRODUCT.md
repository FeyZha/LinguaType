# LinguaType 产品说明

## 产品定位

LinguaType 是面向中文母语英语学习者的“输入法式英文表达助手”。它帮助用户在真实写作现场，把当前最新句中的中文意图、中英混写和不自然英文表达，转换成更自然的英文句子。

LinguaType 不是通用翻译器、聊天机器人、作文生成器、全文批改器、作文评分工具、真实系统输入法、Chrome 扩展或云同步产品。

## 当前版本

当前版本是 v0.2.7。

v0.2.7 在 v0.2.6 主界面工作台基础上继续收敛交互：

- 写作存档侧栏改为可折叠结构，参考 ChatGPT 式历史列表，但仍服务于本地文章草稿。
- 每条写作存档提供低调 `...` 菜单，支持重命名和删除。
- 删除存档需要确认；删除当前存档后切换到剩余最近存档，删除唯一存档后创建空白“未命名写作”。
- 主题、写作领域和大纲修改不再通过主界面抽屉完成，而是嵌入编辑器区域的轻量内联编辑。
- 大纲检查仍只在用户确认保存主题或大纲修改后调用 `/api/check-outline`。

v0.2.6 在 v0.2.5 写作准备和分段写作基础上优化主界面工作台：

- 主界面提供本地写作存档，用户可以新建、切换和重命名写作稿。
- 写作存档保持 localStorage-only，不引入登录、数据库或云同步。
- 已进入主界面后，修改主题和大纲通过主界面轻量交互完成，不离开正文编辑器。
- 主界面不再单独展示文章大纲管理卡片；大纲只嵌入正文段落标题和“修改大纲”入口。
- 大纲默认只读，只有进入明确编辑态后才能编辑。
- `/api/check-outline` 只在用户点击“确定并检查大纲”后调用，不在输入、新增或删除大纲点时自动触发。
- Selection Actions 根据选区动态定位，优先显示在选区附近，并以结构化中文展示解释结果。

v0.2.5 在 v0.2.4 写作准备页基础上做界面和流程细化：

- 写作准备页使用纯中文界面。
- 写作领域直接展示为按钮，不再使用下拉框。
- 每个写作领域提供 10 个本地预设文章主题。
- 文章主题保留输入框，并在右侧提供本地刷新按钮。
- 大纲从单个 textarea 改为多个独立输入框，默认 3 点，可增减。
- 主写作界面按大纲点数量展示对应数量的段落输入框。
- 用户可在主写作界面编辑、增加或减少大纲点。
- 主题设置只放在主写作界面，不放在写作准备页。
- 深色模式补充浮层、建议区和状态提示的可读性样式。
- 新增 `/api/check-outline`，在用户修改大纲后非阻塞检查大纲是否贴合文章主题。

## 核心流程

```text
用户写作
  -> 输入英文或中英混写
  -> 触发最新句增强
  -> POST /api/enhance-fast
  -> 代码生成 diff
  -> Apply / Cancel
  -> Apply 后替换当前最新句
  -> Apply 后后台提取 Learning Library 和 Correction Events
```

最新句增强仍只处理最新非空句，不改写整段，不生成新论点，不自动 Apply，不在用户确认前保存学习数据。

## 写作准备

写作准备页只收集用户提供的写作上下文：

- 写作领域
- 文章主题
- 大纲点

主题刷新只从本地预设主题中抽取，不调用 LLM，不生成正文，也不自动修改大纲。

## 分段写作

主写作界面根据大纲点数量显示对应数量的段落输入框。每个段落输入框前显示对应大纲点，帮助用户按自己的结构写作。

分段输入只是 UI 组织方式，底层草稿仍保存为 `linguatype.writingDraft.v1` 中的文本，方便保持最新句增强、range replacement、Apply 冲突检测和本地草稿恢复的既有行为。

## 写作存档

写作存档用于保存一次次本地写作内容。每个存档包含标题、正文和 Writing Setup。存档标题可以单独重命名，不会自动改写文章主题。

切换存档会保存当前正文和 setup，但不会保存学习数据，不触发 Learning Library、Correction Events 或 `/api/extract-learning`。

写作存档侧栏可以折叠。存档条目的 `...` 菜单支持重命名和删除。删除只影响 `linguatype.writingArchives.v1` 中对应的本地草稿，不会清空 Learning Library、Correction Events 或旧 draft/setup key。

## 大纲检查

用户在编辑器内联修改主题或大纲后，只有点击确认保存才调用 `/api/check-outline`。该检查只判断大纲是否明显偏离文章主题、重复或过少。

规则：

- 不写作文。
- 不生成段落。
- 不决定用户论点。
- 不自动修改大纲。
- 无问题时不显示提示。
- 有问题时只显示简短中文建议。

## 本地优先

v0.2.x 数据保持 localStorage-only，包括 API Settings、writing draft、Writing Setup、Writing Archives、Theme Settings、Learning Library、Correction Events、Paragraph Health cache、Trigger Settings 和 Personal Dictionary。

当前范围不引入登录、数据库、云同步、支付、导入系统或后端持久化。
