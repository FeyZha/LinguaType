# 任务清单

## 1. OpenSpec 与基础梳理
- [x] 1.1 创建本轮 OpenSpec 变更文档、设计和任务清单。
- [x] 1.2 梳理当前主组件、存储类型、API 路由和测试覆盖。

## 2. 基础 UI 系统
- [x] 2.1 增加统一 Heroicons 风格图标组件。
- [x] 2.2 增加统一点击外部关闭浮层工具。
- [x] 2.3 按新版 `DESIGN.md` 调整全局色彩、字体、弱边界、隐藏滚动条和深色主题基础变量。
- [x] 2.4 保留 `animejs` 作为低调动效能力，不引入装饰性动画。

## 3. 左侧存档栏与顶栏
- [x] 3.1 提升 `LinguaType` 和 `写作存档` 的视觉权重，按 R0 风格重做新建入口。
- [x] 3.2 删除“当前存档标题”修改路径，存档显示直接跟随正文标题。
- [x] 3.3 为存档栏增加搜索、排序、筛选、领域堆栈和拖放分类。
- [x] 3.4 将数据管理提到左侧 `写作习惯` 下方同级入口。
- [x] 3.5 将主题图标和 `API 设置` 移到右上角，主题按钮只显示图标。
- [x] 3.6 将设置改名为 `触发设置`，与快捷键帮助排在左下角。

## 4. 编辑器
- [x] 4.1 删除所有显式 `编辑` 按钮并降低标题字号。
- [x] 4.2 标题改为正文流直接编辑，删除标题下方重复主题行。
- [x] 4.3 删除 `增强最新一句` 和 `校对提示 Proofreading` 按钮。
- [x] 4.4 把模式、强度、触发、领域、文本统计和校对数量组织为正文底部状态栏。
- [x] 4.5 选中文本只先显示解释图标，点击后才调用解释接口并显示解释界面。

## 5. 领域分类
- [x] 5.1 为写作归档增加领域、自动分类元数据和用户手动覆盖标记的兼容迁移。
- [x] 5.2 增加低频领域分类 API 和 provider service，限定输出预设领域或 `自定义`。
- [x] 5.3 在切换文章时按字数、内容变化比例和间隔条件触发分类。
- [x] 5.4 用户拖放修改领域后停止该文章的自动分类。

## 6. 表达库和写作习惯
- [x] 6.1 表达库取消写作模式过滤和展示。
- [x] 6.2 表达库按难度 1-5 提供筛选，并在条目上显示难度 tag。
- [x] 6.3 个人词典整合为表达库类型选项。
- [x] 6.4 表达库页面改成去卡片化的沉浸式列表组织。
- [x] 6.5 写作习惯页面改成去卡片化报告流。
- [x] 6.6 写作习惯例子用规整前后对照替代 `->`。

## 7. 文档与验证
- [x] 7.1 更新相关测试，覆盖存档分类、表达库难度、弹层关闭和选择解释入口。
- [x] 7.2 更新产品文档和 OpenSpec live specs。
- [x] 7.3 运行合适验证，至少包含 `npm run lint`、`npm test`、`npm run build`。
