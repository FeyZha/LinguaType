# 任务清单

## 1. OpenSpec 与边界

- [x] 1.1 建立 `long-text-placeholder-suggestion-flow` change。
- [x] 1.2 写清去大纲、长文本写作、中文占位触发和采纳后学习沉淀边界。

## 2. 本地中文占位模型

- [x] 2.1 新增中文占位符识别工具和类型。
- [x] 2.2 新增当前句 range 提取逻辑：只返回含中文占位符的当前句。
- [x] 2.3 支持句号、问号、感叹号、换行和完整句后的 800ms 稳定触发条件。
- [x] 2.4 为占位识别和当前句 range 增加 focused tests。

## 3. 长文本 WritingEditor

- [x] 3.1 将 `WritingEditor` 正文区域改为单个原生 `textarea` 长文本输入面。
- [x] 3.2 移除正文中的 outline heading / `第 N 段自由写作` 渲染。
- [x] 3.3 保留全文 offset 的 selection、cursor、快捷键和表达菜单入口。
- [x] 3.4 保留底部状态栏和本地文本校对显示。

## 4. 中文占位建议流

- [x] 4.1 在 `LinguaTypeApp` 中增加中文占位句稳定检测。
- [x] 4.2 自动触发时只请求含中文占位符的当前句。
- [x] 4.3 保留 `Ctrl/Cmd + Enter` 手动触发；优先处理含中文占位符当前句，纯英文仅保留原有手动兼容路径。
- [x] 4.4 阻止纯英文句子自动调用 AI。
- [x] 4.5 避免相同 range 和相同文本重复触发。

## 5. 内联建议卡

- [x] 5.1 将建议内容调整为“自然英文句子 / 中文表达 -> 英文表达 / 可复用结构”。
- [x] 5.2 支持采纳、忽略、换一种说法。
- [x] 5.3 支持 `Tab` 采纳、`Esc` 忽略、`Ctrl/Cmd + R` 换一种说法。
- [x] 5.4 Apply 仍使用 captured range 和 snapshot conflict detection。

## 6. 学习资产保存

- [x] 6.1 采纳后从建议卡保存表达资产到 Learning Library。
- [x] 6.2 忽略、重新生成、纯显示状态不保存学习资产。
- [x] 6.3 保持 Learning Library 去重规则不变。

## 7. 验证

- [x] 7.1 跑新增 focused tests。
- [x] 7.2 跑 `npm run lint`。
- [x] 7.3 视改动范围跑 `npm test`。
- [x] 7.4 不更新 `CHANGELOG.md`，等本轮整体收尾再统一更新。

## 8. 细节修正

- [x] 8.1 学习保存成功不再在写作区显示文案。
- [x] 8.2 表达库侧边栏入口新增未查看更新圆点，打开表达库后清除。
- [x] 8.3 段落健康提示改为不占页面文档流的短暂浮层，并支持手动忽略。
- [x] 8.4 写作状态栏不再被状态提示和段落健康提示顶离页面底部。
- [x] 8.5 定位单块 `contentEditable` 受控重渲染会导致输入光标跑到文本开头的问题。
- [x] 8.6 将正文输入底层切换为原生 `textarea`，保留全长文本、选择区、快捷键和自动高度，恢复接近 Word / Typora 的正常输入体验。
- [x] 8.7 收紧中文占位 AI 调用：用户只输入中文片段时只显示弱提示，不请求模型；只有当前句已经完整后才允许自动或手动触发建议。
- [x] 8.8 删除写作区内“正在增强最新一句”等加载弹出/占位提示，后台请求不得影响底部状态栏位置。
- [x] 8.9 将自动中文占位建议改为右侧小图标先提示，用户点击后才展开句下内联卡片。
- [x] 8.10 采纳或忽略后保留已调用 AI 的细线标识，支持再次点击回到该句建议卡。
- [x] 8.11 将句号、问号、感叹号、换行调整为进入稳定等待；自动请求必须等完整句稳定 800ms 后再发起。
- [x] 8.12 将文章标题与正文写作列对齐，并保持正文输入面无卡片背景。
- [x] 8.13 将建议入口补齐为右侧图标 + 句下细线；图标与目标句同水平，细线可打开同一内联卡片。
- [x] 8.14 内联卡片去掉“更自然的表达”标题，不重复完整原句；中文意群下划线可高亮对应英文表达。
- [x] 8.15 对整句中文占位收缩学习点，例如 `会火 -> go viral`，避免学习点重复整句。
- [x] 8.16 使用本地浏览器真实输入流程完成视觉验证：标题/正文对齐、自动请求、标识、内联卡片、状态栏底部固定。

## 9. 追加稳定性与动效统一

- [x] 9.1 新建/切换/删除当前档案时清理当前句建议、旧内联卡片、冲突状态和临时标记，避免空白新文档继承旧卡片。
- [x] 9.2 自动后台触发改为扫描全文中所有已完成且含中文的句子，按句子非阻塞并行请求，支持多个小图标和多个句下虚线共存。
- [x] 9.3 快捷键触发改为处理光标所在句；纯英文仍走手动当前句增强，含中文句走中文占位建议。
- [x] 9.4 取消目标文本下方可见虚线和边框，只保留右侧低干扰图标作为建议入口。
- [x] 9.5 采纳、忽略或文本已替换后继续保留已调用建议记录，支持再次点击右侧图标复习原内联卡片。
- [x] 9.6 写作存档列表摘要调整为“领域 · 日期/时间 · 词数”的格式。
- [x] 9.7 表达库页面和列表项改为同一套轻量上浮动效，去除顶部区域与下方列表动效感受不一致的问题。
- [x] 9.8 移除写作区“我看到了”弱提示文案，后台中文占位检测不再产生任何正文附近提示。
- [x] 9.9 移除会干扰光标定位的可见句下标记，仅保留隐形定位层辅助右侧图标跟随目标句。
- [x] 9.10 重构写作页选区高亮色，降低浏览器默认蓝色选择条对深浅主题的割裂感。
- [x] 9.11 已采纳复习态卡片移除“忽略/采纳”，只保留“换一种说法”，并避免在卡片中重复显示目标句。
- [x] 9.12 选中文本后删除单独小圆图标，改为跟随选区的轻量功能条：解释、保存到表达库、复制、关闭。
- [x] 9.13 写作习惯近 7 天修正节奏改为有数据时显示可读柱状内容，无数据时显示轻量空状态。
- [x] 9.14 更新 `README.md`，用中文说明当前 v0.2.8 产品定位、写作链路、本地数据和验证方式。
