# v7 完整版原型回归

## 结论

更新后的内部原型已用冻结 v4 的 20 case 完成真实本地 API 回归，20/20 通过。该结果证明 v7 完整版已经接入产品链路，且产品侧锁定 source 与路由的实现可以运行；不代表英文质量或真实用户效果已经证明。

## 结果

- 模型：DeepSeek V4 Flash 非思考模式。
- Prompt：`expression-scaffold-v7-full-controlled-rules`。
- 调用：20 次，非流式、单次调用、零自动重试。
- 协议：20/20 通过。
- 延迟：平均 1221.5ms，P50 1083ms，P90 1693ms，最大 2314ms。
- 检查：顶层 source 与顺序、产品侧固定路由、复杂项 2—3 个支架、附属成分不独立成项、首屏投影不泄漏隐藏英文。

## 边界

本轮没有判断英文自然度、语义精确性或用户是否真的能继续组句。下一阶段应使用一次真实雅思写作会话验证等待、揭示、取舍、继续编辑和主观反馈，而不是继续优先优化 Prompt 细节。
