# Changelog

本文件记录历史版本变化，不作为当前产品形态的规则来源。当前产品规则以 `PRODUCT.md` 和 `AGENTS.md` 为准。

## v0.3.1

- 将文章地图状态入口移动到底部固定状态栏，替换原“文本校对：N 条提示”的位置；顶部不再漂浮显示文章地图按钮。
- 移除写作区右侧文本校对 tag、右下角校对卡片和本地 proofreading 分析链路；个人词典归一化迁移到 `src/lib/personalDictionary.ts`。
- “检查本段”二级界面的细节问题过滤空格类细碎排版项，避免用 spacing 提示铺满列表。
- 折叠侧栏“检查本段”改用文档检查图标，并为折叠侧栏功能图标补充 hover 说明卡片。
- 新增文章地图自动预检查：本地判断段落数量、正文 hash、主题/大纲 hash 和段落指纹变化，并以 `empty`、`fresh`、`stale`、`needs_check`、`checking`、`ready`、`failed` 管理 freshness。
- 新增文章地图自动检查触发设置：关闭、仅提醒、安静时预检查、手动优先；默认使用安静时预检查。
- 自动预检查仅在至少 2 段、至少 120 英文词、用户停顿约 10 秒、变化幅度达到阈值、没有其他 AI 请求、API 可用、间隔和会话次数限制通过后触发。
- `POST /api/check-document-map` 请求新增 `trigger`，支持 `manual`、`auto_idle`、`after_apply` 和 `after_outline_change`；`auto_idle` prompt 只要求高优先级结构发现。
- 升级 `linguatype.documentMapCache.v1`，补充正文 hash、主题/大纲 hash、段落指纹、freshness、生成时间、自动检查时间和会话内自动检查次数。
- 文章地图入口新增轻状态文案：可检查、整理中、N 个发现、可能已过期；自动结果不会自动展开面板。
- 增加自动预检查测试，覆盖静默后台请求、按钮发现数、manual_first 不自动调用，以及不触发 Paragraph Flow 或学习提取。

## v0.3

- 新增“文章地图”：用户手动点击“检查文章地图”后生成 Office 大纲式全文结构检查面板。
- 新增 `POST /api/check-document-map`，通过 `checkDocumentMapWithLLM` 返回全文主旨、结构判断、段落地图、全文结构问题和优先修改建议。
- 新增 `src/components/DocumentMapPanel.tsx` 与 `src/lib/documentMap.ts`，支持段落切分、range 定位、可折叠段落节点和过期提示。
- 新增 `linguatype.documentMapCache.v1`，按 archive、正文 hash、setup 和 provider 上下文缓存文章地图。
- 文章地图节点整合“查看建议”和“检查本段”，分别复用 Paragraph Health 和 Paragraph Flow 的既有边界。
- 文章地图打开后宽屏改为左右对照布局，避免地图面板把正文整体顶到下方。
- 文章地图左右对照区改为地图栏和原文栏独立隐藏滚动，避免原文滚动带动地图位置。
- 段落健康浮层中的“查看建议”改为只展开轻量健康摘要，不再误触发段落流畅度检查。
- 删除右下角段落健康浮层；文章地图“查看建议”直接在段落卡片内显示轻量健康结果。
- “检查本段”改为进入文章地图二级检查界面，折叠侧栏入口也复用同一界面，并增加运行中动效提示。
- Paragraph Flow 返回新增 `detailIssues`，用于显示本段语法、拼写、标点、冠词、时态、词形、介词、搭配和空格等细节问题。
- 明确文章地图不做全文改写、不评分、不自动应用、不保存学习数据、不触发 `/api/extract-learning`。

## v0.2.8

- 将沉浸式写作重构定名为 v0.2.8。
- 正文输入从分段/大纲驱动的文档表面收敛为单块原生长文本写作面，优先保证光标定位、选区、输入法和追加输入稳定。
- 中文片段改为“表达占位符”处理：完整句稳定后才后台异步准备建议，刚输入中文时不再打断。
- 新增 `linguatype.placeholderSuggestionCache.v1`，已生成的中文占位句建议按文档、原句、模式、强度和领域缓存，重新打开页面时优先恢复而不是重复请求模型。
- 包含中文的目标句先显示右侧低干扰入口，用户打开后才临时拆出目标句并显示内联卡片。
- 已采纳的建议记录可复习，复习态卡片移除忽略和采纳，只保留换一种说法等复习操作。
- 左侧写作存档升级为固定文章库，支持搜索、排序、领域筛选、领域堆栈和拖放分类。
- 顶部只保留图标式主题切换和 `API 设置`；数据管理、触发设置和快捷键帮助移到左侧低频入口。
- 标题改为正文流直接编辑，删除重复主题行、显式编辑按钮和 `增强最新一句` 按钮。
- 底部状态栏整合模式、强度、触发、领域、文本统计和本地校对数量；校对问题改为写作区右侧轻量 tag，hover/focus 时高亮对应正文片段。
- 模式和强度菜单恢复为底部轻量下拉，并统一点击外部或 Escape 收起。
- 选中文本改为显示跟随选区的轻量功能条，支持解释、保存到表达库、复制和关闭。
- 新增低频写作领域自动分类 API；用户拖放改分类后不再自动覆盖。
- 低频管理页反馈收敛：表达库、写作习惯、数据管理、触发设置、API 设置的复制、插入、删除、导出、保存、清空和测试操作统一给中文优先状态反馈，清空与删除保留确认；导出/连接失败不误报成功。
- 表达库取消写作模式关联，改用难度 1-5 和类型筛选，并把个人词典并入类型选项。
- 表达库改为统一小卡片网格，来源句默认截断，点击卡片可在原位展开完整来源，便于长期积累后快速浏览。
- 新增表达复现提示：正文命中本地表达库中的短语或搭配时，以视觉-only 的中性卡片压印动效强化“已学表达被自然写出”的学习反馈，不再显示 hover/focus 说明卡片或常驻线条。
- 段落健康改为始终开启，只保留 Apply 后和段落完成后两种触发；旧手动、关闭和三次 Apply 值迁移到新模式。
- `Ctrl/Cmd + K` 改为直接检查本段，移除旧表达菜单弹窗和触发设置；折叠侧栏保留增强当前句和检查本段图标入口。
- 写作习惯改为报告流，趋势图补齐可见内容，示例用规整前后对照替代 `->`。
- 引入统一 Heroicons 风格图标、弹层外部点击关闭机制，以及随浅色/深色主题切换的本地品牌图片资产。

## v0.2.7

- 主界面更新为沉浸式 Typora 式一体化工作台，降低 header、右侧工具和卡片边框的视觉重量。
- 文章主题以 H1 风格进入正文流，大纲点以段落标题进入正文流，正文输入升级为 `contentEditable` 文档表面。
- 新增 `src/lib/editorDocument.ts`，维护段落拆分、空行拼接、全文 offset 和建议定位。
- 当前句建议改为插入 captured range 所在段落下方的行内 diff 建议条，继续保留 Apply / Cancel / Regenerate / Copy。
- 写作存档侧栏改为可折叠结构，折叠后保留展开和新建写作入口。
- 存档条目增加 `...` 操作菜单，支持重命名和删除。
- 删除存档需要确认；删除当前存档后自动切换到剩余存档，删除唯一存档后创建空白“未命名写作”。
- 主界面移除 Writing Setup 抽屉，主题、写作领域和大纲点改为编辑器内联轻量修改。
- 大纲检查继续保持显式触发：输入中不调用，确认保存主题或大纲后才调用 `/api/check-outline`。
- 删除右侧常驻管理栏，改为左侧固定导航打开中间的表达库、写作习惯和统一设置页。
- `API 设置`、触发与打扰设置、数据管理合并到统一设置页；个人词典并入表达库类型。
- 主题切换改为轻量弹出菜单，只显示浅色、深色和跟随系统，并补齐主布局深色变量与英文输入字体栈。

## v0.2.6

- 新增本地写作存档，支持保存、切换、新建和重命名写作稿。
- 主界面调整为写作存档侧栏、中央编辑器和低频工具区的工作台结构。
- 进入主界面后，主题和大纲修改改为抽屉式 Writing Setup，不离开正文编辑器。
- 主界面移除独立文章大纲管理卡片，大纲只嵌入段落标题和修改入口。
- 大纲检查改为显式触发：只在点击“确定并检查大纲”后调用 `/api/check-outline`。
- Selection Actions 改为根据选区动态定位，删除重复英文标题，并结构化展示解释结果。
- 新增 `linguatype.writingArchives.v1`，继续保留旧 draft 和 setup key。

## v0.2.5

- 写作准备页改为纯中文界面。
- 写作领域从下拉框改为直接展示的领域按钮。
- 每个领域提供 10 个本地预设文章主题，主题输入框右侧增加刷新按钮。
- 大纲从单个 textarea 改为多个独立输入框，默认 3 点，可增减。
- `WritingSetup` 存储升级为 `outlinePoints: string[]`，兼容旧 `outline: string`。
- 主写作界面按大纲点数量展示对应数量的段落输入框，并允许继续编辑大纲。
- 主题设置只放在主写作界面，不再放在写作准备页。
- 修复深色模式下建议区、状态提示和浮层区域的可读性问题。
- 新增 `POST /api/check-outline`，非阻塞检查大纲是否贴合文章主题，只提示不修改。

## v0.2.4

- 增加 Writing Setup 写作准备入口，用户可选择写作领域并填写文章主题和大纲。
- 增加 Theme Preference，支持 Light、Dark、System。
- 新增 `linguatype.writingSetup.v1` 和 `linguatype.themeSettings.v1` 本地存储。
- 保持 latest-sentence enhancement、Apply/Cancel、background learning extraction 和本地学习数据规则不变。

## v0.2.3

- 新增 OpenSpec 配置和中文 spec 写作约定。
- 新增 `PRODUCT.md`、`ARCHITECTURE.md`、`MODULES.md` 和 `CHANGELOG.md`。
- 梳理当前产品范围、应用结构、模块归属和最新句数据流。
- 明确本版本是 documentation-only architecture cleanup，不改变用户体验和业务代码。

## v0.2.2

- 将高频交互移动到编辑器附近。
- 引入当前句建议卡片，用于展示代码生成 diff、简短解释、应用、忽略、换一种说法和复制。
- 增加触发设置、选中文本操作、本地校对信号、个人词典和数据管理。
- 保持表达库、写作习惯、段落健康和段落流畅度的本地优先边界。

## v0.2.1

- 将最新句增强切换到 `/api/enhance-fast`。
- 将 learning extraction 移到 Apply 后后台运行。
- 将原始问题日志升级为写作习惯聚合视图。
- 增加轻量 Paragraph Health check。

## v0.2

- 从单一 latest-sentence enhancer 扩展为本地表达学习助手。
- 引入表达库、Correction Events / 写作习惯、段落流畅度和表达工具方向。
- 保持用户确认后才保存学习数据的原则。

## v0.1

- 建立核心 latest-sentence enhancement flow。
- 只处理最新非空句。
- 使用代码生成 diff。
- 用户必须明确 Apply 或 Cancel。
- Apply 后才替换编辑器文本。
