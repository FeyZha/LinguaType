# LinguaType 当前版本审查导读

更新日期：2026-09-04

## 一句话定义

LinguaType 面向中文母语的雅思写作者：当用户知道中文意思、但无法在当前英文句子中自然表达时，系统提供最低但足够的英文表达支架，帮助用户继续写，同时不替用户完成整句。

## 这次希望你审查什么

请基于仓库内的事实回答三个问题：

1. 当前产品定义、交互链路和模型控制中，最关键的问题与不足是什么？
2. 哪些问题已有证据，哪些只是推测，哪些仍缺真实用户验证？
3. 下一版本最小且可验证的推进路径是什么？请避免直接扩展为全文批改、评分、续写、观点生成或通用聊天。

## 当前产品链路

1. 用户在雅思作文中先用中文占位，继续写作。
2. 系统识别含中文的句子，用户一次只处理一句。
3. 模型读取题目、完整作文和目标句。
4. 产品侧锁定中文片段和帮助路由；模型一次生成所需内容。
5. 简单词语直接显示一个英文表达；复杂片段预生成默认 2 个、最多 3 个“中文语义支架 + 隐藏英文”。
6. 用户点击中文支架后，本地揭示对应英文，不再调用模型。
7. 用户自行决定取舍、词序、词形、连接和接入方式；系统不自动修改作文。

## 当前实现

- 前端：`prototype/` 中的高保真写作工作区。
- 模型：DeepSeek V4 Flash，非思考模式。
- Prompt：`evals/expression-scaffold/prompts/baseline-v7-full-controlled.md`。
- 接口：`POST /api/scaffold`，契约见 `prototype/contracts/scaffold-api.v1.openapi.json`。
- 本地状态：同一设备保存多篇作文、支架、揭示状态与反馈；不含账号与云同步。
- 当前 GitHub 分支与已发布的 `v0.3.1` 旧应用相互独立；旧版不是当前需求或功能的继承来源。

## 已有证据

- 冻结评测集共 20 个 case、25 个顶层中文片段。
- v1—v4 记录了产品规则从“直接给表达”到“一次生成、渐进揭示”的变化。
- DeepSeek v7 原型对冻结 20 case 的本地真实 API 回归为 20/20 通过结构、路由、单次调用与首屏投影检查；P50/P90 约 1.08/1.69 秒。
- 产品所有者完成过 1 次真实写作自测，结果是正向定性信号：支架帮助继续写作，最终整合仍由用户完成。
- 高保真前端已合入当前项目；运行源码和定向测试位于 `prototype/`。

## 证据边界

- 20 个冻结 case 中只有 1 个继承真实卡点，且上下文经过重建；其余 19 个来自 AI 初拟和静态复审。
- 结构或协议通过不等于英文质量通过，也不等于用户价值已经证明。
- 只有 1 名产品所有者、1 次真实会话，不能外推目标用户普遍效果。
- v1—v7 的任务定义不同，不能把不同版本数字直接写成能力提升。
- 当前字符阈值路由只在冻结样本中匹配，真实语义复杂度泛化尚未验证。
- 部分复杂支架的语义边界仍依赖 Prompt；尚无独立的 v7 全量英文质量结论。
- 当前没有外部用户测试、长期使用数据、跨设备同步或业务指标。

## 建议阅读顺序

1. `README.md`：项目全貌、版本边界与导航。
2. `AGENTS.md`：当前不可突破的产品边界和证据口径。
3. `docs/PRODUCT-REASONING.md`：需求如何收敛为当前产品锚点。
4. `docs/MVP-IMPLEMENTATION-CHAIN.md`：产品链路、状态与失败边界。
5. `prototype/FRONTEND-BRIEF.md`：高保真交互目标。
6. `prototype/README.md` 与 `prototype/app/`：当前实现。
7. `evals/expression-scaffold/README.md`：全部评测版本、实验和运行结果入口。
8. `evals/expression-scaffold/eval-sets/`、`prompts/`、`runs/`：核验具体证据。

## 本地运行

```text
cd prototype
npm ci
npm run dev
```

页面可直接查看；真实模型调用需要按 `prototype/.env.example` 从仓库外提供环境变量。不要把 API Key 写入仓库。

定向测试：

```text
node --test tests/*.test.mjs
npm run contract:check
```

## 审查要求

- 先区分“事实、合理推断、待验证假设”。
- 不把 AI 初拟样本当成真实用户证据。
- 不把离线协议通过写成英文质量或产品效果。
- 优先建议最小验证闭环，不先增加第二条高频链路。
- 若建议改变产品主方向，请明确标注为需要用户决策，而不是直接当作下一步。
